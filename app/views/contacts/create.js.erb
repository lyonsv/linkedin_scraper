<% if @contact.new_record? %>
  alert("Failed to create contact profile: <%= j @contact.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $("#contacts").preppend("<%= j render(@contact) %>");
<% end %>
